apiVersion: v1
kind: Service
metadata:
  name: {{ include "clearing-house.fullname" . }}
  labels:
    {{- include "clearing-house.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "clearing-house.selectorLabels" . | nindent 4 }}
